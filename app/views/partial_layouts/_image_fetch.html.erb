<%# BIGTODO: Render images from our database %>
<img  class="<%= class_name.present? ? class_name : '' %>" 
      width="<%= width %>" 
      height="<%= height %>"
      <%# TODO::The image object URL doesn't appear here %>
      src=`${imageObjecUrl}`
      >